#include <iostream>

using namespace::std;

int main()
{	
	 int x = 5;
	 
	 cout << "\nx = " << x;
	 cout << "\nx++ = " << x++;
	 cout << "\n++x = " << ++x;
	 
	 int a, b, c;
	 a = 1;
	 b = 0;
	 c = a || b;
	 cout << "\na && b = " << c;
	 
	 
	 return 0;
}

